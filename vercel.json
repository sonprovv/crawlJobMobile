{
  "version": 2,
  "env": {
    "NODE_OPTIONS": "--max_old_space_size=4096"
  },
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "build" }
    }
  ]
}
